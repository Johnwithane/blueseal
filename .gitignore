node_modules/
dist/
dist-ssr/
.secrets/
.env
.env.local
.env.*.local
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
e2e/.artifacts/
.playwright-mcp/
*.log
functions/lib/
functions/node_modules/
.cache/
# Local-only Claude Code state. Agents + skills ARE tracked — remote sessions
# (claude.ai/code) only get what's committed, and the /bugs triage skill and
# the scout/test-writer/qa-runner agents are part of how this repo is worked.
.claude/settings.local.json
*.tsbuildinfo
functions/set-admin.mjs

# Mascot source art — only the optimized .webp ships (run npm run optimize:characters)
public/characters/png/
public/characters/raw/

# Prospect audit exports — contain harvested business contact data; never commit
prospect-audit.json
prospect-audit.csv
